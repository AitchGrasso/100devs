String.prototype.toJadenCase = function () {
// P: making a method that  turns something into jadencase. You will be replacing every first letter of the work touppercase
// R: should be a string coming back with every work starting with a uppercase letter
// E: Not Jaden-Cased: "How can mirrors be real if our eyes aren't real"
// Jaden-Cased:     "How Can Mirrors Be Real If Our Eyes Aren't Real"
// P: use split on the string .split(""), forEach for each element, take ele[1].toUppercase, join string, return joined sting

  return this.split(" ").map(ele => ele[0].toUpperCase() + ele.slice(1)).join(" ");

};